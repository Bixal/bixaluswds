{#
/**
 * @file
 * Theme override for a field: entity reference revisions.
 */
#}
{% extends 'field.html.twig' %}

{% block content %}
  {% if not label_hidden %}
    <div{{ title_attributes.addClass(title_classes) }}>{{ label }}</div>
  {% endif %}
  {% for item in items %}
    {{ item.content }}
  {% endfor %}
{% endblock %}
